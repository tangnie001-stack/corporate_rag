# P3 词项命中探针（jieba + PG 全文检索 横向比较）

- 语料：176 个分块 / 5 个知识库
- 检索条数 k：30（= TOP_K_RETRIEVAL，上限 100）
- 探针词项数：30（n-gram 2..4，df ∈ [2, 0.1×N]）

> **仅供相对比较，不得作为质量基线或发布判据。**
> 176 分块属**小语料**；探针的用途是在同一语料上横向比配置，**既不测排序质量也不测语义召回**。

## 口径说明（读表前必读）

- **词项来源**：从**原始正文**抽 CJK 连续段的 2..4 元 n-gram，不经任何分词器 —— 避免同一分词器既造索引又造标签的自我循环。
- **命中判据**：原始正文的字符串包含（`str.__contains__`），与分词器无关。
- **词项筛选**：`2 <= df <= 0.1×N`，剔除 df=1 与高频项；再按 `(df 升序, 词项)` 取**最稀有**的若干项。**该取舍使词项偏向最难召回的一端，各臂命中率是偏保守（偏病态）的下界，不得读作平均检索质量。**「平凡通过」只在词项 df 较大、或池规模接近 k 时才成立，与本探针取样方向相反，不适用。
- **单库钉定**：词项被钉在「第一个含它的 kb」上、只在该库内检索，多库语料下进一步压低命中率 —— 同属**使结果更病态**的取舍。
- **排序是否决定成员，按臂分两类**：**过滤臂**（`prefix-AND` / `prefix-OR` / `plainto-AND` / `jieba-tsrank` / `substring`）候选集由 WHERE 谓词决定，`_term_kb` 保证所选库含该词项、候选规模 ≤ k=30，`LIMIT k` 返回全部匹配行 ⇒ 对排序**不敏感**（分组 C 零差是**结构必然**）；**未过滤的 BM25 臂**（`char-bm25` / `jieba-bm25`）对**整池**排序取 top-30（池 ≫ k：各库 2/1/1/51/121，大库 51/121 远超 k），**排序决定成员**，两臂 top-30 随 tokenization 不同而不同。
- **本口径能裁决什么**：**能**登记 A1 的差分（已登记）；**不能**评判其好坏（判据是「原始正文是否包含」，非排序质量）；**不能**裁决分组 C（候选集相同）；分组 B 的 `prefix-OR` / `plainto-AND` 差异是**谓词覆盖面**的差，非排序质量。

## 分组 A1：分词配置（唯一同口径对比：char-bm25 vs jieba-bm25）

两臂除**分词方式**外完全同口径：同 BM25Okapi、同词项集、同在每库全池上排序（不过滤候选）、同 k。

| 配置 | 命中数/总数 | 词项命中率 |
|---|---|---|
| char-bm25 | 30/30 | 1.0000 |
| jieba-bm25 | 29/30 | 0.9667 |

- 实测：`char-bm25` 30/30 = 1.0000；`jieba-bm25` 29/30 = 0.9667。
- 差值 = **1 个词项 = 3.33 个百分点**（n=30，每词项 1/30），方向是 **char 更高** —— 与「jieba 更好」相反，故**本次替换未观察到命中收益**。
- ⚠️ `char-bm25` 的满值属**构造性必然**：字符级索引以单字为单元，而探针查询 n-gram 逐字取自源文档，命中近乎必然。**1.0 不能证明「字符级检索质量更好」**；同理，也不能据此宣称 jieba 有收益。
- ⚠️ 该差值**确是排序差异**（两臂都在 >k 的池上排序取 top-k，tokenization 不同 ⇒ top-k 成员不同，见口径说明）；但本口径**不能把它判为「质量」差** —— 命中判据是原始正文包含，且 `char-bm25` 的满值属构造性必然（见上）。

## 分组 A2：混合变量行（仅供参考，**不可归因**）

以下两行相对 A1 **同时改了不止一个变量**，故不能与 A1 并列解读：

- `jieba-tsrank`：同时改了**候选生成**（先 `tsv @@ tsquery` 过滤，而非在每库全池上排序）与**打分**（`ts_rank`）。
- `pg-trgm`：同时改了**打分函数**（`similarity()`）与**切分单位**（trigram）。

| 配置 | 命中数/总数 | 词项命中率 |
|---|---|---|
| jieba-tsrank | 15/30 | 0.5000 |
| pg-trgm | 28/30 | 0.9333 |

- `jieba-tsrank` = 15/30 = 0.5000，相对 A1 的下降**主要由候选生成（tsquery 过滤）解释**：查询词元在该库的 tsv 里不存在时谓词直接为空，与分词把它切没切碎无关。**不可归因于分词质量。**
- `jieba-tsrank` 是生产落地形态（存储侧依赖 `scripts/rewrite_content_seg.py` 的重写结果）；`pg-trgm` 是本地唯一可对照的扩展方案，两者**都不构成对分词方案的证据**。

## 分组 B：查询构造（固定分词 = jieba，固定打分 = ts_rank）

| 配置 | 命中数/总数 | 词项命中率 |
|---|---|---|
| prefix-AND | 15/30 | 0.5000 |
| prefix-OR | 18/30 | 0.6000 |
| plainto-AND | 14/30 | 0.4667 |
| substring（定义性，非策略对比） | 30/30 | 1.0000 |

- `substring` = 30/30 = 1.0000 是**定义性**行（**恒为 1.0，非策略对比**）：`_term_kb` 已保证所选 kb 含该词项，且词项 df 远小于 k=30，故 `content LIKE '%term%' ... LIMIT k` **必然**返回全部含该词项的行 —— 命中是 WHERE 子句的同义反复。该行还是一次**无索引全表扫描、且无相关性排序**（`ORDER BY doc_id, chunk_index` 只提供确定性输出顺序，并非得分排序），**不是可选策略，不得读作「LIKE 最好」**。
- `substring` 臂相对 `prefix-AND` **改了不止一个变量**：构造（`LIKE` vs `tsquery`）之外还改了排序（`ORDER BY doc_id, chunk_index` vs `ts_rank`）。
- `prefix-OR` vs `prefix-AND`：18/30 vs 15/30，差 **10.0 个百分点**。OR 是 AND 的**超集谓词**；在判据为「集合成员」而非「排序」、且候选集近饱和的口径下，这是**机械后果而非质量证据**。
- `plainto-AND` = 14/30 = 0.4667，低于 `prefix-AND` 3.3 个百分点，同样受「排序不敏感」影响。

## 分组 C：打分算法（固定 jieba + 前缀 AND）

| 配置 | 命中数/总数 | 词项命中率 |
|---|---|---|
| ts_rank | 15/30 | 0.5000 |
| ts_rank_cd | 15/30 | 0.5000 |

- 实测：`ts_rank` 15/30 = 0.5000；`ts_rank_cd` 15/30 = 0.5000。
- ⚠️ 零差不表示两个打分函数「同分」，而是：**候选集相同且规模 ≤ k 时该口径对排序不敏感**，函数差异**在此口径下无法测出**（见口径说明）；比较打分质量需候选集远大于 k 并比对 top-k 排序，本探针未做。

## 显式失效用例（探针的 df 筛选测不到，必须单列）

每条 `EXPLICIT_CASES` 成员都**必须**出现在下表；语料不含该串的用例记为 `corpus-absent`，不得静默消失。

| 查询 | 走的路径 | 是否命中 | kb |
|---|---|---|---|
| 涨了吗 | corpus-absent | 未验证（语料不含该串） | — |
| 5 月 | substring | ✅ | ea84fb72… |
| 增长 | prefix-AND | ✅ | 2f85ec3c… |
| 营业收入 | prefix-AND | ✅ | 2f85ec3c… |
| 资产负债率 | prefix-AND | ✅ | ea84fb72… |
| 净利润 | prefix-AND | ✅ | 095efa16… |

## 选型结论

- 查询构造：**prefix-OR**（分组 B 命中数/总数：prefix-AND = 15/30 / prefix-OR = 18/30 / plainto-AND = 14/30 / substring = 30/30（定义性））
- 打分算法：**ts_rank**（分组 C 命中数/总数：ts_rank = 15/30 / ts_rank_cd = 15/30）
- 分词方案：**jieba + `to_tsvector('simple')`**（分组 A1 命中数/总数：char-bm25 = 30/30 / jieba-bm25 = 29/30）

- **连接符翻转（改选 prefix-OR）**：依据**不是**分组 B 的 +10.0 个百分点 —— OR 是 AND 的超集谓词，集合成员口径近饱和，+10pp 是其**机械后果**，不构成质量证据（见分组 B 说明，该对比数据作为对照记录保留）。真正依据是**服务路径实测**：生产前缀 AND 下，自然语言查询被改写为「资产负债率 变化 趋势」，而语料不含 `变化`/`趋势` ⇒ AND 谓词为空 ⇒ `sparse_count=0`，**词法路对典型多词自然查询贡献为零**（"混合检索"退化为纯 dense），D7 仅靠精心挑选的单概念查询才成立。故 `_JOINER` 由 `" & "` 改为 `" | "`，精度交由下游 RRF / 去重 / rerank 承担。
- **打分函数不翻转**：分组 C 零差来自「该口径对排序不敏感」，非质量证据，故保持 `func.ts_rank(`。

## 词项清单（完整，供复现）

```
一季度报 一季度财 一季起 一季起调 一年度 一搜广 一搜广告 一日止 一日止第 一日的 一日的平 一步 一步应 一步应用 一直 一直秉 一直秉承 一般企 一般企业 一般及 一般及行 一般风 一般风险 一节 一节重 一节重要 丁晓 丁晓仑 丁红 七节
```
