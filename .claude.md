## 类型注解要求

所有代码必须写完整类型注解，不准用 `Any` 或 `object`。

- 返回类型写具体类型
- 参数类型写具体类型
- 字典/列表写内部元素类型（`dict[str, int]`、`list[str]`）
- `Optional` 用 `X | None` 或 `Optional[X]`
- **不要用 `Any` 或 `object` 代替不确定的类型**，用联合类型 `type | type | type` 穷举所有可能取值
  - 例如：`dict | None`、`str | None`、`list[dict] | None`
