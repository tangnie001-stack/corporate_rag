services:
  app:
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
      - ./skills:/app/skills
      - ./agents:/app/agents

  nginx:
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./deploy/nginx/html:/usr/share/nginx/html
